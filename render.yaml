# Render Blueprint — deploys both services in one step.
# In the Render dashboard: New → Blueprint → connect this repo → Render reads
# this file and proposes the two services below. Secret-valued env vars
# (marked sync: false) are left blank for you to fill in per-service in the
# dashboard — never committed here.
#
# The admin app (internal job postings tool) lives in admin/ but is
# deliberately NOT pushed to this repo or listed here — it's kept local /
# deployed separately. If it's ever pushed to its own repo, give it the same
# two-envVar static-site block the frontend below has (rootDir: admin).
#
# https://render.com/docs/blueprint-spec

services:
  # --- Backend: Express API (Supabase + Razorpay + Claude/Gemini/Groq) ---
  - type: web
    name: aifagen-backend
    runtime: node
    rootDir: backend
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: RAZORPAY_WEBHOOK_SECRET
        sync: false
      - key: RAZORPAY_CURRENCY
        value: USD
      - key: CORS_ORIGIN
        value: https://aifagenlabs.com,https://www.aifagenlabs.com

  # --- Frontend: public Vite/React app ---
  - type: web
    name: aifagen-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      # SPA fallback — React Router owns client-side routes like /dashboard.
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false
      - key: VITE_API_URL
        value: https://api.aifagenlabs.com
