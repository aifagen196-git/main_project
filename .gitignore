# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Build
dist/
build/

# Environment
.env
.env.*
frontend/.env
backend/.env
!.env.example
!**/.env.example

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Coverage
coverage/

# Temporary
tmp/
temp/

# Stale backup archives — duplicate source already tracked as real files
job-engine.zip
frontend.zip
job-engine/samples/indeed-search.zip

# Kept locally, not pushed — internal tooling / one-off scripts / docs not
# meant for the deploy repo (per explicit request). Still present on disk;
# just untracked.
/verify/
/admin/
/DEPLOY.md
/IMPLEMENTATION_NOTES.md

# job-engine/ is tracked here (this folder is the consolidated primary
# repo) — it also exists as its own separate repo
# (github.com/aifagen196-git/collectors) for its independent GitHub
# Actions cron; both are kept in sync manually.
job-engine/node_modules/
job-engine/.env
job-engine/logs/
job-engine/*.log