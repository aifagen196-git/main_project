# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Build
dist/
build/

# Environment
.env
.env.*
frontend/.env
backend/.env
!.env.example
!**/.env.example

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Coverage
coverage/

# Temporary
tmp/
temp/

# Stale backup archives — duplicate source already tracked as real files
job-engine.zip
frontend.zip

# Kept locally, not pushed — internal tooling / one-off scripts / docs not
# meant for the deploy repo (per explicit request). Still present on disk;
# just untracked.
/verify/
/admin/
/DEPLOY.md
/IMPLEMENTATION_NOTES.md

# job-engine/ is NOT tracked here — it's its own repo
# (github.com/aifagen196-git/collectors) with its own GitHub Actions cron,
# kept as the single source of truth rather than duplicating and manually
# syncing it across two repos. Still present on disk in this folder too;
# just untracked.
/job-engine/