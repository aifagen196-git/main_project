// config/indeedSearches.js
//
// Reuses LinkedIn's keyword list — these are generic occupation titles, not
// anything LinkedIn-specific, so a second copy would just be duplication.
export { default } from "./linkedinSearches.js";
